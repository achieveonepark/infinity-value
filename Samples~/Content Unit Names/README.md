# Content Unit Names

Import this sample to see custom unit names modeled as content instances.

`UnitNameContent` is a ScriptableObject that owns one `InfinityValueUnitNames` instance and exposes helper methods for creating and parsing values with that instance.
